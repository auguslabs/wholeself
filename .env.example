# Variables de entorno - EJEMPLO (sin valores reales)
# Copia este archivo a .env y rellena con tus valores. NUNCA subas .env al repositorio.

# API (para formularios y servicios)
PUBLIC_API_URL=https://api.example.com

# Deploy estático Bluehost: origen del sitio (sin barra final). Los formularios envían a PUBLIC_API_BASE/api/forms/*.php
# Ejemplo producción: https://wholeselfnm.com  |  Desarrollo: http://localhost:4321
PUBLIC_API_BASE=https://wholeselfnm.com

# Base de datos (MySQL - Fase 1 formularios)
# Formato típico: mysql://usuario:password@host:3306/nombre_bd
DATABASE_URL=mysql://user:password@localhost:3306/wholeself_forms

# reCAPTCHA (panel admin y formularios)
# Site key: puede ser pública en el front. Secret key: SOLO en servidor.
PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here

# Contenido desde BD (páginas y equipo). true = leer desde MySQL; false = leer desde JSON.
# En producción (build para Bluehost): PUBLIC_USE_CONTENT_FROM_BD=true
# En LOCAL (npm run dev): pon PUBLIC_USE_CONTENT_FROM_BD=false en .env.local para usar JSON y no conectar a MySQL remoto (evita "Access denied").
# PUBLIC_USE_CONTENT_FROM_BD=true

# Deploy Bluehost: NO poner aquí. Usar GitHub Secrets (FTP_SERVER, FTP_USERNAME, FTP_PASSWORD).
